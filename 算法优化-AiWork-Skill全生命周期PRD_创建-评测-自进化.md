# Skill 全生命周期 PRD · 创建 → 评测 → 自进化

| | |
|---|---|
| 文档类型 | 产品需求文档 |
| 作者 | AI 策略产品经理 |
| 日期 | 2026-09-05 |
| 关联文档 | `Skill创建-评测-进化的产品动作设计.md`（完整设计）、`skill-creator_评测报告_S3阻断与7条缺陷.md`（实证数据） |
| 实证对象 | `skill-creator`（被测）、`skill-evolve.plugin`（评测工具） |

---

## 摘要

> **一句话结论**：“创建 → 评测 → 自进化”三段式链路成立，但本次实证发现它的**第一瓶颈不在 skill 质量，而在度量工具自身的可信度**——我们用来把关的校验器会说谎。

**三个关键数字**

| 数字 | 读法 |
|---|---|
| 缺陷数 **1 → 3 → 0** | 修好校验器之后，被测对象的缺陷数**先涨了 2 条**，再降到 0。修尺子的收益不是让分数变好看，而是让分数**先变难看、变得可信** |
| 生态样本 **47 个 skill，0 个有评分标准** | 这是本链路要填的市场空白 |
| 进度 **7 条缺陷，5 条已闭环，2 条卡在用户输入** | 剩下 2 条按设计不能由 agent 代做 |

**当前唯一卡点**：`skill-creator` 处于 **S4（空锚点）**状态，缺用户口述的四个答案：为什么建它 / 验收时看什么 / 哪些请求不该触发 / 什么产出直接判不合格。这一步 agent 不能代填——代填等于让机器自己批准自己的标准。

---

## 1. 为什么做：一条不可逆的因果链

```
skill 生成成本趋近于零
  → 数量爆炸（生态样本：47 个 skill 中 0 个有评分标准）
  → 人工无法逐个验收
  → 劣质 skill 与优质 skill 在路由层同权竞争
  → 用户体验被平均值拖垮，而非被最差值拖垮
  → 平台对“skill 可信”这件事失去定价能力
```

最后一条是商业后果：当平台无法区分好坏，它就不能对“可信”收费，也不能把不可信的下架。于是优质供给退出，剩下的是“看起来都能用、实际没人敢用”。

**关键判断：这不是质量问题，是信息缺失问题。** 我们无法淘汰劣质 skill，不是因为判断力不足，而是因为判断所需的三项信息在时间上不可逆地丢失了：

| 丢失点 | 因果后果 | 为什么不可逆 |
|---|---|---|
| **意图** | 评分标准只能拿 skill 自己写的说明书去反推 → 相当于开卷考自己出的题 → 任何照着做的 agent 都能满分 | 创建会话一结束，“用户当初为什么建它”永久消失 |
| **标准的合法性** | 拦截发生时被拦者问“这标准谁定的”，答不上来 → 该次拦截失效 → 连带此后所有拦截被质疑 | 事后补的标准无法证明它代表用户价值而非 AI 猜测 |
| **迭代方向** | 改动无法区分“进步”与“改坏了” → 每次优化都是赌博 | 没有基准版本快照（指纹），就没有可比性 |

**所以必须三段式，且三段之间是串行门禁（上线卡点），不是加权评分。** 加权会让“生成得漂亮”补偿“根本不该被触发”——而后者是乘法链上的归零项：一旦为 0，总分再高也没意义。

---

## 2. 产品动作设计

### 2.1 仓库布局

```text
skill-lifecycle/
├─ skill-creator/                 【创建】独立 skill，不装在插件里
│   ├─ SKILL.md                   七步流程
│   ├─ agents/                    3 个 subagent：盲评 / 揭盲归因 / 逐条判定
│   ├─ scripts/                   10 个：交付门禁、格式检查、指纹、跑评测、聚合 benchmark…
│   ├─ references/                4 篇：产物分档 / 契约与评分切分 / schema / description 优化
│   └─ evals/intent.yaml          它自己的锚点
├─ skill-evolve.plugin/           【评测 + 进化】一个插件装两个 skill
│   └─ skills/
│       ├─ skill-eval/            四态定级 + 标准门禁 + 4 个模板 + 自己的 4 条用例
│       └─ skill-evolve/          版本门禁 + 4 篇归因/可比性/状态参考
└─ tests/                         仓库级共享测试（不属于任何单个 skill）
    ├─ run_tests.py               12 个回归测试
    └─ fixtures/                  5 个样例 skill + 11 个运行结果场景
```

两个布局事实值得注意：

- **`skill-eval` 与 `skill-evolve` 各有一份 `anchor.py`**：锚点协议是两个 skill 共享的契约，这与它们被装进同一个插件、而 `skill-creator` 独立在外是一致的。
- **测试在仓库级，不在 skill 内**。这是对的——它要同时验证三个 skill 与它们之间的交接；但也正是这一点让我上一轮误判为“零测试覆盖”（只搜了插件目录内部）。

### 2.2 端到端链路：从一句话到可发布的版本决策

三个 skill 不是并列关系，而是**三个档位递进**，每跳一档都需要一个外部输入：

```text
用户口述意图
  │  skill-creator：意图澄清 → 分流 → 写两份文件 → 静态校验
  ▼
【T0 可用 + 有锚点】 SKILL.md（契约）+ intent.yaml（锚点）
  │  ↑ 这一档 agent 可独立完成
  │  skill-eval：四态定级（只有 S1 放行）→ 编译 → 定位 → 选型 → 写配置 → 标准门禁
  ▼
【T1 可评测】 eval.yaml + cases/*.yaml，且 rubric_status = 已审定
  │  ↑ 跳档必需：**用户逐条审定**（agent 不得代做）
  │  skill-eval 第七步：skill-up run → result.json
  ▼
【T2 有基线】 一次真实运行的分数
  │  ↑ 跳档必需：**真实跑一次**（本机缺 CLI 时这一档到不了）
  │  skill-evolve：归因 → 受控改写 → 对照验证 → 版本门禁
  ▼
版本决策（发布 / 回滚 / 让用户知情同意这次交换）→ 回到 skill-eval 重新采样
```

**为何要分三档而不是一次交全套**：默认 T0。生态实测 47 个 skill 里只有 1 个带 evals，T0 是生态的实际常态，不是降级方案；而 T0 的产物正是 T1/T2 的必要前提，不存在浪费。反过来，把没审定过的评分标准塞给用户，会制造“看起来能评测”的假象。

### 2.3 三阶段分工

| 阶段 | 谁 | 输入 | 产物 | 门禁（卡点） | 明令禁止 |
|---|---|---|---|---|---|
| **创建** | `skill-creator` | 用户口述意图 | `SKILL.md`（给 agent 看的操作规程）+ `evals/intent.yaml`（价值锚点） | `quality_gate` 返回通过 | 起草评分标准、打分 |
| **评测** | `skill-eval` | `intent.yaml` | 评分配置 + 测试用例 + 基线分（baseline） | `rubric_gate` 通过 **+ 用户逐条审定** | 从 `SKILL.md` 反推标准、自己把自己标成“已审定” |
| **进化** | `skill-evolve` | 基线分 + 失败归因 | 补丁 + 四组对照 + 版本门禁决策 | 不得使任一已通过的用例转红 | 无对照直接改 |

两个词先定义清楚，下文反复用：

- **锚点**（`evals/intent.yaml`）= 用户意图的书面存档。存的是“当初为什么建、验收看什么”，而不是“skill 怎么写”。
- **指纹** = 对 `SKILL.md` 全文算的内容哈希。改一个字就变，用来判断“标准还对不对得上当前这个版本”。

### 2.4 四条设计决策及其因果

**D1 · 评分标准与被测对象物理隔离**（落在 `evals/` 目录，绝不写进 `SKILL.md`）
→ 因为三条后果同时成立：① **自我证明**（生成器给自己出题）；② **应试污染**（被测 agent 读到评分点，就会针对评分点作答，而不是针对真实需求）；③ **对照组失效**（基线组不装被测 `SKILL.md`，如果标准写在里面，两组用的就不是同一把尺子）。
→ 且这不是口头约定而是**代码层的结构保证**：评测引擎在加载 skill 时硬编码跳过整个 `evals/` 目录，且这个跳过发生在用户自定义排除规则之前——意味着**即使配置错了也泄不出去**。

**D2 · 意图在创建期落盘，不在评测期补**
→ 除了不可再生，还有一条：`SKILL.md` 一旦生成，用户再看评分标准时会被 skill 内容锚定（看到它写了什么，就觉得应该考什么），澄清就失去了独立性。

**D3 · 评分标准走三格状态机 `未起草 → 已起草 → 用户已审定`，agent 永不能自推最后一格**
→ 机器自己批准的标准，不能用来卡机器自己的产出。工具层对此硬性拒绝，不是靠提示词约束。

**D4 · 锚点绑定 `SKILL.md` 的指纹**
→ 内容一改，旧的审定自动失效（工具对失效态硬性拒绝重新盖章）。理由：一个记录在错误版本上的审定，比没有审定更危险，因为下游门禁会信任它。

### 2.5 锚点四态 S1–S4：评测的前置门

D2–D4 落到运行时就是这四个状态。工具在任何评测动作之前先给锚点定级，它决定流程能不能往下走：

| 状态 | 判据 | 含义 | 处置 |
|---|---|---|---|
| **S1 完整** | 锚点齐全 + 验收标准是用户真话 + 指纹与当前 `SKILL.md` 一致 | **唯一可产出分数的状态** | 直接进入评分标准起草 |
| **S2 失效**（漂移） | 内容齐全，但指纹对不上——盖完章之后 `SKILL.md` 被改过 | 旧标准描述的可能已是另一个 skill | **停**，带用户复审：只是改措辞 → 重新盖章沿用原标准；动了能力范围 → 标准置失效，重新起草 |
| **S3 裸态** | 锚点文件根本不存在 | 用户意图从未被记录，且**已不可再生** | **停**，回头问用户四个问题；**绝对禁止**从 `SKILL.md` 反推 |
| **S4 空锚点** | 文件在，但验收标准仍是模板占位符，**或**指纹从未盖过 | 结构就绪，内容或手续缺 | 按 S3 处理，但告知用户“只需补几个字段” |

两个易混点：

- **S3 与 S4 的差别是恢复成本，不是严重级别。** 两者都阻断、都禁止反推；S3 要重新访谈用户（所以是“最贵的一档”），S4 只要补字段或补一条命令。
- **锚点四态与评分标准状态是两条正交的轴。** 前者量的是“意图存下来了吗”（S1–S4），后者量的是“标准合法吗”（未起草 → 已起草 → 已审定，即 D3）。两者必须分开读：本次实证里 `skill-creator` 是 **S4 + 未起草**，`skill-eval` 是 **S4 + 已起草**。同为 S4，前者缺内容（需用户访谈），后者只缺手续（一条命令），处置成本差一个量级。

### 2.6 从意图到测评指标：四级生成链路

这是 2.2 那条链里“T0 → T1”这一跳的内部细节。四级，每级只回答一个问题：

| 级 | 做什么 | 在哪一步 | 判据 |
|---|---|---|---|
| **① 分流** | 把澄清出来的内容拆到两份文件 | `skill-creator` 第三步 | 这段文字被 agent 读到，会让它**行为更好**，还是**针对考点作答**？ |
| **② 编译** | 锚点字段 → 可判定断言 | `skill-eval` 第一步 | 映射是**机械的**，不允许临场发挥 |
| **③ 定位** | 评分标准放哪个物理位置 | `skill-eval` 第二步 | 按长度与复用需求选三处之一 |
| **④ 选型** | 用哪种判定器 | `skill-eval` 第三步 | 成本阶梯：能便宜判的绝不用贵的 |

**① 分流：一句判据决定两份文件**

| 去向 | 内容 |
|---|---|
| `SKILL.md`（契约） | 成功标准、不适用范围、执行约束、正触发样例 |
| `intent.yaml`（锚点） | 用户为什么建它（痛点）、验收原话、负触发样例 |

验收原话不能进 `SKILL.md` 的理由有两条：它会编译成“不得出现 SWOT”这类断言，写进说明书等于**把考点公布给被测 agent**；且开 benchmark 时基线组不装 `SKILL.md`，两组就不是同一把尺子。

**② 编译：三个字段三种命运**

| 锚点字段 | 编译成 | 为何 |
|---|---|---|
| `user_acceptance[]`（验收原话） | 评分条目或确定性断言 | 直接可判定 |
| `trigger_samples.negative[]`（负例） | 负例用例的输入 prompt | 测路由混淆 |
| `user_value.pain`（痛点） | **不编译成断言** | 痛点是“为何建”，不是“产出该长什么样” |

痛点只用于**校准**：如果起草的用例与痛点无关（痛点是“手工翻 8 个网站花 2 小时”，用例却在测“能不能解析 PDF”），说明跑偏了。

编译时有两条铁律：

1. **保留原话的可判定部分，剥离不可判定部分。** “太啰嗦了”要编译成“结论前置，单个产品分析不超过 5 句”，而不是“输出简洁”——后者会让同一份产出跑三次得三个分数，**不是 skill 在变，是尺子在抖**。
2. **禁止型断言必须配一个正向断言。** 只写“不得出现 SWOT”，agent 输出一个烂矩阵也能过；只测“没有 X”会让一个**什么都没做的空输出通过**。

**③ 定位：三个位置，默认选最轻的**

| 位置 | 什么时候用 |
|---|---|
| 写进用例的 `judge.criteria` | 短标准（≤7 条）、单个用例专用。**默认选这个** |
| 独立的评分标准包，用 `judge.skills` 挂载 | 长标准、多个用例共享同一把尺子 |
| `expect` + 规则判定 | 可确定性判定的（文件在不在、关键词、退出码、正则） |

第二种位置的机制价值就是 D1 的物理实现：`judge.skills` **只装进评分 agent，不装进被测 agent**；而开 benchmark 时实验组与基线组**都会装它**——它被认定为评分工具而非被测对象。这就是“两组用同一把尺子”的实现方式。

**④ 选型：成本阶梯，便宜的先用**

| 要判定的东西 | 用 | 成本 |
|---|---|---|
| 文件在不在、关键词、退出码 | `expect` | 零，且不通过时**直接跳过评分** |
| 输出结构符正则、必须调过某工具 | 规则判定 | 极低 |
| 解析 JSON、算指标、自定义逻辑 | 脚本判定 | 低 |
| 需要语义理解（“建议是否可操作”） | agent 评分 | **每次都烧 token，且引入评分噪声** |

原则：先用便宜手段覆盖能确定性判定的部分，**只把真正需要语义理解的留给 agent 评分**。

**谁来判：三个 subagent 的角色分离**

| 角色 | 职责 | 分离的理由 |
|---|---|---|
| 盲评员（comparator） | 比较两份产出，**不知道哪份来自哪个 skill** | 防止偏向某个 skill 或某种写法 |
| 揭盲归因（analyzer） | 盲评定出赢家后才揭盲，分析**为何赢**、输的怎么改 | 先定胜负再找原因，避免结论先行 |
| 逐条判定（grader） | 对每条断言给出通过/失败与证据，**并同时批判断言本身** | 它的设计原话：“在弱断言上给出通过，比没有用还糟——它制造假信心” |

最后一条把本文反复出现的“假信心”主题内置进了工具：判定器不只判产出，还要判**题目出得好不好**。

**指标怎么读：三条陷阱**

- **要不要开对照组**：问“现在多少分”不开；问“到底有没有用、比不用强多少”才开，且必须告知成本翻倍（每个用例跑两遍）。
- **对照组结论有前提**：两组共用同一把尺子，所以分差**可以**归因于 skill 本身——但前提是第 ① 级分流没把考点泄漏进 `SKILL.md`。泄漏了就是实验组偷看了尺子，分差里混着信息不对称，不能说明 skill 有用。
- **分数不稳时**：同一用例跑 3 次看方差。方差大说明标准里有不可判定条目，回去修标准，**不要靠多跑几次取平均来掩盖**。

**终点：版本门禁的四条阻断**

改动前后两份运行结果静态比对（不花 token），四种情况直接阻断而非警告：

| 阻断规则 | 为何不是警告 |
|---|---|
| 回归：原本通过的用例变失败 | 老用例代表已被认可过的能力。丢掉它换取新能力是**一次交换而不是一次改进**，必须让用户知情并同意 |
| 标准未经用户审定却要给发布结论 | 门禁的合法性来自用户审定 |
| `SKILL.md` 指纹已漂移但标准仍标“已审定” | 用过期合格证放行 |
| 两份结果的引擎/模型配置不一致 | 差值不可比，任何结论都建立在错觉上 |

另有三条仅警告：分差落在标准差内、出现新的不稳定用例、运行时模型未知。

**门禁不提供 `--force`。** 有回归而确实需要发布时，正确做法是让用户明确同意这次交换、并把同意记进变更日志，而不是让脚本闭嘴。

---

## 3. 实证链路：评测 skill-creator

本节四个动作。每一步都有可重跑的命令与返回码，不是事后描述。

### 动作 1 · 评测：三道门全部阻断

| 命令 | 返回 | 它说什么 | 白话 |
|---|---|---|---|
| `quick_validate .` | **0**（通过） | `"Skill is valid!"` | 只查格式，很浅 |
| `quality_gate .` | **1**（拦截） | `gate: BLOCKED` | 真正的交付门禁，判定不可交付 |
| `anchor check-drift` | **1**（拦截） | `S3 bare — no anchor` | 锚点文件不存在 |

结论 `BLOCKED-S3`（四态定义见 2.5）。**本轮不产出任何分数**——锚点缺失时产出分数等于伪造。

注意前两行：两道门对同一个 skill 给出了**相反的结论**，而 skill 自己的说明书只让用户跑通过的那一道。

### 动作 2 · 归因：三层根因，危险度递增

| 层 | 归因 | 危险度 |
|---|---|---|
| 流程性 | 缺锚点文件（S3 裸态，恢复成本最高的一档） | 中：流程会停下，人能察觉 |
| **工具性** | **校验器有“代码块盲区”**：它逐行找标题，但不认 Markdown 代码块（用三个反引号包起来的示例区）。于是 skill-creator 写在**教学模板**里的示例标题，被当成了它自己拥有的章节 | **高：流程带着错误结论继续走，人无法察觉** |
| 生态性 | 评测工具自身不完整（引用断裂、姊妹 skill 为空） | 中 |

用一句话概括工具性根因（本次最有价值的产出）：

> 任何“教别人写 X”的元 skill，都会自动通过“你自己有没有 X”这项检查。

因为它为了教人，必然把 X 的模板写在正文里；而检查器分不清“这是它的模板”和“这是它自己的内容”。

**更麻烦的是，本该拦住它的规则一条都没响**：模板里的占位符恰好带了箭头符号，绕过了“必须给替代方案”这条；示例条数刚好 2 条，既没超过路由章节“最多 3 条”的上限，也没超过边界章节“最多 5 条”的上限。**没有一条规则是坏的，它们只是都在看错的地方。**

（技术细节备查：该函数逐行判断 `line.startswith("#")`，全程不维护代码块的开/合状态；受影响函数名为 `bullets_under`，共 2 个调用点。）

同时暴露一对**虚荣指标**：`quick_validate` 通过与 `quality_gate` 拦截 **共存**，而 `SKILL.md` 第六步原文只让用户跑前者。→ 把“校验通过率”当发布指标是危险的：分子分母都可以被一个浅检查刷高。

### 动作 3 · 策略：先修尺子，再量东西

顺序不可颠倒。若先补内容再修校验器，所有中间测量都无效；若不修校验器，补完内容也无法验证。

| 决策 | 选择 | 理由 |
|---|---|---|
| 修复顺序 | 工具性 → 流程性 → 生态性 | 只有工具性缺陷会制造**假信心**；其余两类至少会让流程停下来 |
| 改公共函数还是加新函数 | **加新函数** | 同一段文本有两个用途，且要求相反：**查泄题**时必须连代码块一起看（被测 agent 读的是整个文件，藏在模板里的违禁词一样是把答案印在考卷上）；**查章节齐不齐**时必须跳过代码块。改公共函数会顺手把泄题检查改坏 |
| 具体落点 | 新增一个“只看正文、跳过代码块”的视图函数，仅在章节检查处调用 | 影响面已量化：受影响的调用点只有 2 处，且都在同一个函数内 |
| 要不要加测试 | **加，但目标不是覆盖率** | 项目原本就有一套回归测试，**12/12 全绿**，还故意种了 6 个缺陷在里面。但它种的全是“**该有的东西没有**”这一类；本次这个 bug 属于“**看着有、其实是假的**”。那个用来测试的假 skill 全文只有 **13 行、一个代码块都没有**，结构上根本不可能触发这个 bug。**全绿的测试套件反而制造了假信心** |

### 动作 4 · 修复 + 复测

6 项动作，共约 +520 / −15 行：

| 缺陷 | 动作 | 白话 |
|---|---|---|
| B1 校验器盲区 | 新增视图函数（+33/−2） | 让检查器学会跳过示例区 |
| A2 缺边界声明章节 | 补真实的 `## When NOT to Use`（3 条带箭头）+ `## 核心边界`（5 条） | 写清“什么时候不该用我” |
| A3 两道门结论矛盾 | 第六步改成跑两条命令，`quality_gate` 成为**唯一**交付判据 | 消除“说明书让你跑浅检查”这个误导 |
| A1 缺锚点 | 建空锚点文件 + 盖指纹 | S3 → S4（仅手续，内容仍需用户） |
| C3 工具自身裸态 | 给 `skill-eval` / `skill-evolve` 各建锚点 | 先要求自己做到，再去要求别人 |
| 防复发 | 新建对抗样本测试文件，10 个用例 | 专门构造“看着有、其实是假的”样例 |

---

## 4. 关键数据

### 4.1 门禁前后对比

| 检查项 | 修复前 | 修复后 |
|---|---|---|
| `quality_gate`（交付门禁） | **拦截：返回 1 / BLOCKED / 1 条 ERROR** | **通过：返回 0 / PASS / 0 条 ERROR** |
| `quick_validate`（格式检查） | 通过 | 通过（不变，它本来就查不出问题） |
| 锚点状态 | **S3 裸态**（无锚点文件） | **S4 空锚点**，指纹已盖且与当前版本一致 |
| `rubric_gate`（评分标准门禁） | 流程第 0 步即停，**根本没能执行** | **首次可执行**，阻断点后移 |
| 对抗样本测试（专测“伪存在”） | **无**（已有 12/12 套件只测“缺失”） | **+10/10 通过** |
| `skill-eval` 引用断裂 | 5 | **0** |
| `skill-evolve` 内容 | 空目录 0 文件 | 217 行 SKILL.md / 9 文件 / 引用断裂 0 |
| 项目原有回归套件 | 12/12 | **12/12**（带着本次修复仍全绿） |

最后一行是本次修复的**独立验证**。那套回归测试不是我写的，它同时确认了两件事：合格的 skill 仍被判合格（未引入误拦），已种的 6 个缺陷仍被全部报出（未削弱原有拦截）。

为何必须找第三方：**被修对象就是校验器本身，所以不能用它自证。** 拿一把刚修过的尺子去证明它自己修对了，没有信息量。

### 4.2 最重要的一组：修好校验器后，缺陷数先涨后降

| 阶段 | ERROR | WARN | 门禁结论 |
|---|---|---|---|
| 原始（校验器在说谎） | **1** | 0 | 拦截 |
| 仅修校验器 | **2** | **1** | 拦截 |
| 修完全部 | **0** | 1 | **通过** |

> **缺陷数 1 → 3 → 0。** 修尺子的收益不是让分数变好，而是让分数**先变差、变得可信**。
> 产品含义：任何“修完度量工具，指标立刻变漂亮”的情况都应当被当成**待查风险**，而不是成果。

### 4.3 缺陷收敛

7 条缺陷 → **5 条已闭环**（A2/A3/B1/C1/C2）+ **2 条推进到 agent 无法独立完成的状态**（A1/C3 均 S3→S4，剩下的内容按设计只能由用户提供）。

注意这里的用词差别：“闭环”和“推进”不是一回事。后两条并没有完成，只是从“文件不存在”变成了“文件存在但内容空着”——阻断依然在，只是恢复成本从重新访谈降到了补几个字段。

---

## 5. 结论与下一步

### 结论

三段式生命周期成立，且**串行门禁 + 意图锚点 + 物理隔离**这三个机制各自堵住一个不可逆的信息损失点（见 §1 表），缺一不可。

但本次实证的主要产出不在这套机制本身，而在一条反直觉的读数（见 §4.2）：**我们原以为瓶颈是 skill 质量，实测发现瓶颈是度量工具自身的可信度。** 这也决定了下一阶段的投入方向——先修尺子，再谈优化 skill。

### 下一步动作（按依赖排序）

| 优先级 | 动作 | 阻塞方 | 验收判据 |
|---|---|---|---|
| P0 | 用户提供四个答案：为什么建它 / 验收看什么 / 哪些请求不该触发 / 什么产出直接判不合格 | **用户** | 锚点状态 S4 → **S1 完整** |
| P0 | `skill-eval` 补盖自己的指纹（内容已达标，零成本，一条命令） | 无 | 自身状态 S4 → S1 |
| P1 | 锚点就绪后起草评分标准 → 写评测配置与用例（**正例负例 ≥1:1**）→ 过标准门禁 | 依赖 P0 | `rubric_gate` 返回通过 |
| P1 | 安装 skill-up 命令行工具（本机无编译环境） | 用户 | 版本号可查 |
| P2 | 用户逐条审定评分标准 | **用户** | 标准状态置为“已审定” |
| P2 | 实跑基线分，同一用例跑 3 次看方差 | 依赖 P1+P2 | 产出结果文件，且三次分数的方差可解释 |

正负例 1:1 这条要单独说明：负例必须是**近邻**（看起来像但不该触发的请求）。“帮我写首诗”这种远例测不出任何路由混淆，只会把命中率刷高。

### 未解决项（诚实声明）

- **“该不该被触发”这一环测不了**：评测配置在加载阶段就固定了要装哪些 skill，因此无法在同一次运行里安装本次运行刚生成的那个。需外挂两段式流程（第一轮产出文件 → 脚本改写配置指向它 → 第二轮测路由）。**这是结构性缺失，不是配置写错**，靠调参解决不了。
- **本轮未产出任何分数。** 当前所有结论均来自静态门禁与代码级归因，**不含 agent 实跑数据**。引用本文数据时请带上这个限定。
